// Generated by golem_sdk_tools — DO NOT EDIT!

///|
impl @schema.HasElementSchema for ClassLevelInfo with element_schema() {
  @schema.make_record_schema([
    (
      "id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
  ])
}

///|
impl @schema.FromExtractor for ClassLevelInfo with from_extractor(e) {
  let id :String = @schema.extract_field(e, 0)
  let name :String = @schema.extract_field(e, 1)
  { id, name }
}

///|
impl @schema.FromElementValue for ClassLevelInfo with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for ClassLevelInfo with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.id),
    @schema.to_element_value_as(self.name),
  ])
}

///|
impl @schema.HasElementSchema for UserProfileInfo with element_schema() {
  @schema.make_record_schema([
    (
      "auth_id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "role",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "class_level",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "created_at",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
  ])
}

///|
impl @schema.FromExtractor for UserProfileInfo with from_extractor(e) {
  let auth_id :String = @schema.extract_field(e, 0)
  let role :String = @schema.extract_field(e, 1)
  let class_level :String = @schema.extract_field(e, 2)
  let created_at :String = @schema.extract_field(e, 3)
  { auth_id, role, class_level, created_at }
}

///|
impl @schema.FromElementValue for UserProfileInfo with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for UserProfileInfo with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.auth_id),
    @schema.to_element_value_as(self.role),
    @schema.to_element_value_as(self.class_level),
    @schema.to_element_value_as(self.created_at),
  ])
}

///|
impl @schema.HasElementSchema for CreateUserResponse with element_schema() {
  @schema.make_record_schema([
    (
      "pk",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[Int])),
    ),
    (
      "uuid",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "username",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "email",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "is_active",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[Bool])),
    ),
    (
      "groups",
      @schema.schema_of_tag(
        (@schema.TypeTag::{  } : @schema.TypeTag[Array[String]]),
      ),
    ),
  ])
}

///|
impl @schema.FromExtractor for CreateUserResponse with from_extractor(e) {
  let pk :Int = @schema.extract_field(e, 0)
  let uuid :String = @schema.extract_field(e, 1)
  let username :String = @schema.extract_field(e, 2)
  let name :String = @schema.extract_field(e, 3)
  let email :String = @schema.extract_field(e, 4)
  let is_active :Bool = @schema.extract_field(e, 5)
  let groups :Array[String] = @schema.extract_field(e, 6)
  { pk, uuid, username, name, email, is_active, groups }
}

///|
impl @schema.FromElementValue for CreateUserResponse with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for CreateUserResponse with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.pk),
    @schema.to_element_value_as(self.uuid),
    @schema.to_element_value_as(self.username),
    @schema.to_element_value_as(self.name),
    @schema.to_element_value_as(self.email),
    @schema.to_element_value_as(self.is_active),
    @schema.to_element_value_as(self.groups),
  ])
}

///|
impl @schema.HasElementSchema for CacheData with element_schema() {
  @schema.make_variant_schema([
    (
      "Profile",
      Some(
        @schema.schema_of_tag(
          (@schema.TypeTag::{  } : @schema.TypeTag[StudentProfile]),
        ),
      ),
    ),
    (
      "Subjects",
      Some(
        @schema.schema_of_tag(
          (@schema.TypeTag::{  } : @schema.TypeTag[Array[SubjectInfo]]),
        ),
      ),
    ),
    (
      "Terms",
      Some(
        @schema.schema_of_tag(
          (@schema.TypeTag::{  } : @schema.TypeTag[Array[TermInfo]]),
        ),
      ),
    ),
    (
      "Lessons",
      Some(
        @schema.schema_of_tag(
          (@schema.TypeTag::{  } : @schema.TypeTag[Array[LessonInfo]]),
        ),
      ),
    ),
    (
      "LessonContent",
      Some(
        @schema.schema_of_tag(
          (@schema.TypeTag::{  } : @schema.TypeTag[LessonContent]),
        ),
      ),
    ),
    (
      "ClassGroups",
      Some(
        @schema.schema_of_tag(
          (@schema.TypeTag::{  } : @schema.TypeTag[Array[TeacherClassGroup]]),
        ),
      ),
    ),
    (
      "ActiveSessionTerm",
      Some(
        @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
      ),
    ),
    (
      "SessionTerms",
      Some(
        @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
      ),
    ),
  ])
}

///|
impl @schema.FromExtractor for CacheData with from_extractor(e) {
  let (idx, payload) = @schema.extract_variant(e)
  match idx {
    0 => {
      guard payload is Some(p) else {
        raise  @common.AgentError::InvalidInput(
          "Expected payload for CacheData::Profile",
        )
      }
      let field0 :StudentProfile = @schema.from_extractor_as(p)
      Profile(field0)
    }
    1 => {
      guard payload is Some(p) else {
        raise  @common.AgentError::InvalidInput(
          "Expected payload for CacheData::Subjects",
        )
      }
      let field0 :Array[SubjectInfo] = @schema.from_extractor_as(p)
      Subjects(field0)
    }
    2 => {
      guard payload is Some(p) else {
        raise  @common.AgentError::InvalidInput(
          "Expected payload for CacheData::Terms",
        )
      }
      let field0 :Array[TermInfo] = @schema.from_extractor_as(p)
      Terms(field0)
    }
    3 => {
      guard payload is Some(p) else {
        raise  @common.AgentError::InvalidInput(
          "Expected payload for CacheData::Lessons",
        )
      }
      let field0 :Array[LessonInfo] = @schema.from_extractor_as(p)
      Lessons(field0)
    }
    4 => {
      guard payload is Some(p) else {
        raise  @common.AgentError::InvalidInput(
          "Expected payload for CacheData::LessonContent",
        )
      }
      let field0 :LessonContent = @schema.from_extractor_as(p)
      LessonContent(field0)
    }
    5 => {
      guard payload is Some(p) else {
        raise  @common.AgentError::InvalidInput(
          "Expected payload for CacheData::ClassGroups",
        )
      }
      let field0 :Array[TeacherClassGroup] = @schema.from_extractor_as(p)
      ClassGroups(field0)
    }
    6 => {
      guard payload is Some(p) else {
        raise  @common.AgentError::InvalidInput(
          "Expected payload for CacheData::ActiveSessionTerm",
        )
      }
      let field0 :String = @schema.from_extractor_as(p)
      ActiveSessionTerm(field0)
    }
    7 => {
      guard payload is Some(p) else {
        raise  @common.AgentError::InvalidInput(
          "Expected payload for CacheData::SessionTerms",
        )
      }
      let field0 :String = @schema.from_extractor_as(p)
      SessionTerms(field0)
    }
    _ => raise  @common.AgentError::InvalidInput("Unknown CacheData case")
  }
}

///|
impl @schema.FromElementValue for CacheData with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for CacheData with to_element_value(self) {
  match self {
    Profile(field0) => 
      @schema.make_variant_value(0U, Some(@schema.to_element_value_as(field0)))
    Subjects(field0) => 
      @schema.make_variant_value(1U, Some(@schema.to_element_value_as(field0)))
    Terms(field0) => 
      @schema.make_variant_value(2U, Some(@schema.to_element_value_as(field0)))
    Lessons(field0) => 
      @schema.make_variant_value(3U, Some(@schema.to_element_value_as(field0)))
    LessonContent(field0) => 
      @schema.make_variant_value(4U, Some(@schema.to_element_value_as(field0)))
    ClassGroups(field0) => 
      @schema.make_variant_value(5U, Some(@schema.to_element_value_as(field0)))
    ActiveSessionTerm(field0) => 
      @schema.make_variant_value(6U, Some(@schema.to_element_value_as(field0)))
    SessionTerms(field0) => 
      @schema.make_variant_value(7U, Some(@schema.to_element_value_as(field0)))
  }
}

///|
impl @schema.HasElementSchema for CacheItem with element_schema() {
  @schema.make_record_schema([
    (
      "data",
      @schema.schema_of_tag(
        (@schema.TypeTag::{  } : @schema.TypeTag[CacheData]),
      ),
    ),
    (
      "fetched_at",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[UInt64])),
    ),
  ])
}

///|
impl @schema.FromExtractor for CacheItem with from_extractor(e) {
  let data :CacheData = @schema.extract_field(e, 0)
  let fetched_at :UInt64 = @schema.extract_field(e, 1)
  { data, fetched_at }
}

///|
impl @schema.FromElementValue for CacheItem with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for CacheItem with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.data),
    @schema.to_element_value_as(self.fetched_at),
  ])
}

///|
impl @schema.HasElementSchema for SubjectInfo with element_schema() {
  @schema.make_record_schema([
    (
      "id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "code",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
  ])
}

///|
impl @schema.FromExtractor for SubjectInfo with from_extractor(e) {
  let id :String = @schema.extract_field(e, 0)
  let name :String = @schema.extract_field(e, 1)
  let code :String? = @schema.extract_field(e, 2)
  { id, name, code }
}

///|
impl @schema.FromElementValue for SubjectInfo with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for SubjectInfo with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.id),
    @schema.to_element_value_as(self.name),
    @schema.to_element_value_as(self.code),
  ])
}

///|
impl @schema.HasElementSchema for StudentProfile with element_schema() {
  @schema.make_record_schema([
    (
      "class_level",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
  ])
}

///|
impl @schema.FromExtractor for StudentProfile with from_extractor(e) {
  let class_level :String = @schema.extract_field(e, 0)
  { class_level, }
}

///|
impl @schema.FromElementValue for StudentProfile with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for StudentProfile with to_element_value(self) {
  @schema.make_record_value([@schema.to_element_value_as(self.class_level)])
}

///|
impl @schema.HasElementSchema for TermInfo with element_schema() {
  @schema.make_record_schema([
    (
      "id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "sort_order",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[Int])),
    ),
    (
      "active",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[Bool])),
    ),
  ])
}

///|
impl @schema.FromExtractor for TermInfo with from_extractor(e) {
  let id :String = @schema.extract_field(e, 0)
  let name :String = @schema.extract_field(e, 1)
  let sort_order :Int = @schema.extract_field(e, 2)
  let active :Bool = @schema.extract_field(e, 3)
  { id, name, sort_order, active }
}

///|
impl @schema.FromElementValue for TermInfo with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for TermInfo with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.id),
    @schema.to_element_value_as(self.name),
    @schema.to_element_value_as(self.sort_order),
    @schema.to_element_value_as(self.active),
  ])
}

///|
impl @schema.HasElementSchema for LessonInfo with element_schema() {
  @schema.make_record_schema([
    (
      "id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "topic_title",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "week",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[Int?])),
    ),
    (
      "active",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[Bool?])),
    ),
    (
      "subject_name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "term_name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
  ])
}

///|
impl @schema.FromExtractor for LessonInfo with from_extractor(e) {
  let id :String = @schema.extract_field(e, 0)
  let topic_title :String? = @schema.extract_field(e, 1)
  let week :Int? = @schema.extract_field(e, 2)
  let active :Bool? = @schema.extract_field(e, 3)
  let subject_name :String? = @schema.extract_field(e, 4)
  let term_name :String? = @schema.extract_field(e, 5)
  { id, topic_title, week, active, subject_name, term_name }
}

///|
impl @schema.FromElementValue for LessonInfo with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for LessonInfo with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.id),
    @schema.to_element_value_as(self.topic_title),
    @schema.to_element_value_as(self.week),
    @schema.to_element_value_as(self.active),
    @schema.to_element_value_as(self.subject_name),
    @schema.to_element_value_as(self.term_name),
  ])
}

///|
impl @schema.HasElementSchema for LessonContent with element_schema() {
  @schema.make_record_schema([
    (
      "id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "topic_title",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "week",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[Int?])),
    ),
    (
      "subject_name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "term_name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "objectives",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "content_sections",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "key_points",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "mcq_questions",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "theoretical_questions",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
  ])
}

///|
impl @schema.FromExtractor for LessonContent with from_extractor(e) {
  let id :String = @schema.extract_field(e, 0)
  let topic_title :String? = @schema.extract_field(e, 1)
  let week :Int? = @schema.extract_field(e, 2)
  let subject_name :String? = @schema.extract_field(e, 3)
  let term_name :String? = @schema.extract_field(e, 4)
  let objectives :String? = @schema.extract_field(e, 5)
  let content_sections :String? = @schema.extract_field(e, 6)
  let key_points :String? = @schema.extract_field(e, 7)
  let mcq_questions :String? = @schema.extract_field(e, 8)
  let theoretical_questions :String? = @schema.extract_field(e, 9)
  {
    id,
    topic_title,
    week,
    subject_name,
    term_name,
    objectives,
    content_sections,
    key_points,
    mcq_questions,
    theoretical_questions,
  }
}

///|
impl @schema.FromElementValue for LessonContent with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for LessonContent with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.id),
    @schema.to_element_value_as(self.topic_title),
    @schema.to_element_value_as(self.week),
    @schema.to_element_value_as(self.subject_name),
    @schema.to_element_value_as(self.term_name),
    @schema.to_element_value_as(self.objectives),
    @schema.to_element_value_as(self.content_sections),
    @schema.to_element_value_as(self.key_points),
    @schema.to_element_value_as(self.mcq_questions),
    @schema.to_element_value_as(self.theoretical_questions),
  ])
}

///|
impl @schema.HasElementSchema for TeacherSubjectPair with element_schema() {
  @schema.make_record_schema([
    (
      "edge_id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "class_level_id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "class_level_name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "subject_id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "subject_name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "subject_code",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
  ])
}

///|
impl @schema.FromExtractor for TeacherSubjectPair with from_extractor(e) {
  let edge_id :String = @schema.extract_field(e, 0)
  let class_level_id :String = @schema.extract_field(e, 1)
  let class_level_name :String = @schema.extract_field(e, 2)
  let subject_id :String = @schema.extract_field(e, 3)
  let subject_name :String = @schema.extract_field(e, 4)
  let subject_code :String? = @schema.extract_field(e, 5)
  {
    edge_id,
    class_level_id,
    class_level_name,
    subject_id,
    subject_name,
    subject_code,
  }
}

///|
impl @schema.FromElementValue for TeacherSubjectPair with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for TeacherSubjectPair with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.edge_id),
    @schema.to_element_value_as(self.class_level_id),
    @schema.to_element_value_as(self.class_level_name),
    @schema.to_element_value_as(self.subject_id),
    @schema.to_element_value_as(self.subject_name),
    @schema.to_element_value_as(self.subject_code),
  ])
}

///|
impl @schema.HasElementSchema for TeacherClassGroup with element_schema() {
  @schema.make_record_schema([
    (
      "class_level_id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "class_level_name",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "subjects",
      @schema.schema_of_tag(
        (@schema.TypeTag::{  } : @schema.TypeTag[Array[TeacherSubjectPair]]),
      ),
    ),
  ])
}

///|
impl @schema.FromExtractor for TeacherClassGroup with from_extractor(e) {
  let class_level_id :String = @schema.extract_field(e, 0)
  let class_level_name :String = @schema.extract_field(e, 1)
  let subjects :Array[TeacherSubjectPair] = @schema.extract_field(e, 2)
  { class_level_id, class_level_name, subjects }
}

///|
impl @schema.FromElementValue for TeacherClassGroup with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for TeacherClassGroup with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.class_level_id),
    @schema.to_element_value_as(self.class_level_name),
    @schema.to_element_value_as(self.subjects),
  ])
}

///|
impl @config.ConfigField for SharedConfig with collect_entries(path) {
  let entries :Array[@common.AgentConfigDeclaration] = []
  let fp0 = path.copy()
  fp0.push("surreal_host")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp0,
    ),
  )
  let fp1 = path.copy()
  fp1.push("surreal_ns")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp1,
    ),
  )
  let fp2 = path.copy()
  fp2.push("surreal_database")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp2,
    ),
  )
  let fp3 = path.copy()
  fp3.push("surreal_username")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp3,
    ),
  )
  let fp4 = path.copy()
  fp4.push("surreal_password")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp4,
    ),
  )
  let fp5 = path.copy()
  fp5.push("authentik_host")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp5,
    ),
  )
  let fp6 = path.copy()
  fp6.push("authentik_api_token")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp6,
    ),
  )
  let fp7 = path.copy()
  fp7.push("auth_key")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp7,
    ),
  )
  entries
}

///|
impl @config.ConfigField for SharedConfig with load(path) {
  let fp0 = path.copy()
  fp0.push("surreal_host")
  let surreal_host :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp0,
  )
  let fp1 = path.copy()
  fp1.push("surreal_ns")
  let surreal_ns :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp1,
  )
  let fp2 = path.copy()
  fp2.push("surreal_database")
  let surreal_database :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp2,
  )
  let fp3 = path.copy()
  fp3.push("surreal_username")
  let surreal_username :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp3,
  )
  let fp4 = path.copy()
  fp4.push("surreal_password")
  let surreal_password :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp4,
  )
  let fp5 = path.copy()
  fp5.push("authentik_host")
  let authentik_host :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp5,
  )
  let fp6 = path.copy()
  fp6.push("authentik_api_token")
  let authentik_api_token :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp6,
  )
  let fp7 = path.copy()
  fp7.push("auth_key")
  let auth_key :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp7,
  )
  {
    surreal_host,
    surreal_ns,
    surreal_database,
    surreal_username,
    surreal_password,
    authentik_host,
    authentik_api_token,
    auth_key,
  }
}

