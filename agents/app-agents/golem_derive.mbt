// Generated by golem_sdk_tools — DO NOT EDIT!

///|
impl @schema.HasElementSchema for ActivationStatus with element_schema() {
  @schema.make_enum_schema(["NotFound", "Active", "Suspended", "Deactivated"])
}

///|
impl @schema.FromExtractor for ActivationStatus with from_extractor(e) {
  let idx = @schema.extract_enum(e)
  match idx {
    0 => NotFound
    1 => Active
    2 => Suspended
    3 => Deactivated
    _ => {
      raise  @common.AgentError::InvalidInput(
        "Unknown ActivationStatus case: " + idx.to_string(),
      )
    }
  }
}

///|
impl @schema.FromElementValue for ActivationStatus with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for ActivationStatus with to_element_value(self) {
  match self {
    NotFound => @schema.make_enum_value(0U)
    Active => @schema.make_enum_value(1U)
    Suspended => @schema.make_enum_value(2U)
    Deactivated => @schema.make_enum_value(3U)
  }
}

///|
impl @schema.HasElementSchema for UserActivation with element_schema() {
  @schema.make_record_schema([
    (
      "user_id",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "role",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String])),
    ),
    (
      "status",
      @schema.schema_of_tag(
        (@schema.TypeTag::{  } : @schema.TypeTag[ActivationStatus]),
      ),
    ),
    (
      "class_level",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[String?])),
    ),
    (
      "activated_at",
      @schema.schema_of_tag((@schema.TypeTag::{  } : @schema.TypeTag[UInt64])),
    ),
  ])
}

///|
impl @schema.FromExtractor for UserActivation with from_extractor(e) {
  let user_id :String = @schema.extract_field(e, 0)
  let role :String = @schema.extract_field(e, 1)
  let status :ActivationStatus = @schema.extract_field(e, 2)
  let class_level :String? = @schema.extract_field(e, 3)
  let activated_at :UInt64 = @schema.extract_field(e, 4)
  { user_id, role, status, class_level, activated_at }
}

///|
impl @schema.FromElementValue for UserActivation with from_element_value(v) {
  @schema.from_extractor_as(@extractor.extract_component_model_value(v))
}

///|
impl @schema.ToElementValue for UserActivation with to_element_value(self) {
  @schema.make_record_value([
    @schema.to_element_value_as(self.user_id),
    @schema.to_element_value_as(self.role),
    @schema.to_element_value_as(self.status),
    @schema.to_element_value_as(self.class_level),
    @schema.to_element_value_as(self.activated_at),
  ])
}

///|
impl @config.ConfigField for GatewayConfig with collect_entries(path) {
  let entries :Array[@common.AgentConfigDeclaration] = []
  let fp0 = path.copy()
  fp0.push("auth_key")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp0,
    ),
  )
  entries
}

///|
impl @config.ConfigField for GatewayConfig with load(path) {
  let fp0 = path.copy()
  fp0.push("auth_key")
  let auth_key :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp0,
  )
  { auth_key, }
}

///|
impl @config.ConfigField for SurrealConfig with collect_entries(path) {
  let entries :Array[@common.AgentConfigDeclaration] = []
  let fp0 = path.copy()
  fp0.push("host")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp0,
    ),
  )
  let fp1 = path.copy()
  fp1.push("ns")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp1,
    ),
  )
  let fp2 = path.copy()
  fp2.push("database")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp2,
    ),
  )
  let fp3 = path.copy()
  fp3.push("username")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp3,
    ),
  )
  let fp4 = path.copy()
  fp4.push("password")
  entries.append(
    @config.collect_entries_of(
      (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
      fp4,
    ),
  )
  entries
}

///|
impl @config.ConfigField for SurrealConfig with load(path) {
  let fp0 = path.copy()
  fp0.push("host")
  let host :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp0,
  )
  let fp1 = path.copy()
  fp1.push("ns")
  let ns :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp1,
  )
  let fp2 = path.copy()
  fp2.push("database")
  let database :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp2,
  )
  let fp3 = path.copy()
  fp3.push("username")
  let username :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp3,
  )
  let fp4 = path.copy()
  fp4.push("password")
  let password :@config.Secret[String] = @config.load_field(
    (@schema.TypeTag::{  } : @schema.TypeTag[@config.Secret[String]]),
    fp4,
  )
  { host, ns, database, username, password }
}

