// Generated by golem_sdk_tools — DO NOT EDIT!

///|
pub(all) struct StudentAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn StudentAgentClient::get() -> StudentAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::get("StudentAgent", constructor_data)
  { client, }
}

///|
pub fn StudentAgentClient::new_phantom() -> StudentAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::new_phantom("StudentAgent", constructor_data)
  { client, }
}

///|
pub fn StudentAgentClient::get_phantom(phantom_id : @rpcTypes.Uuid) -> StudentAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = 
    @rpc.AgentClient::get_phantom("StudentAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn StudentAgentClient::get_agent_id(self : StudentAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn StudentAgentClient::phantom_id(self : StudentAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn StudentAgentClient::drop(self : StudentAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] StudentAgentClient::scoped(
  f : (StudentAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = StudentAgentClient::get()
  defer client.drop()
  f(client)
}

///|
pub fn[T] StudentAgentClient::scoped_new_phantom(
  f : (StudentAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = StudentAgentClient::new_phantom()
  defer client.drop()
  f(client)
}

///|
pub fn[T] StudentAgentClient::scoped_get_phantom(
  phantom_id : @rpcTypes.Uuid,
  f : (StudentAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = StudentAgentClient::get_phantom(phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn StudentAgentClient::test_db(self : StudentAgentClient) -> String raise @common.AgentError {
  let result = 
    self.client.invoke_and_await("test_db", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_test_db(self : StudentAgentClient) -> Unit {
  self.client.invoke("test_db", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn StudentAgentClient::schedule_test_db(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "test_db",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn StudentAgentClient::schedule_cancelable_test_db(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "test_db",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub(all) struct TeacherAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn TeacherAgentClient::get() -> TeacherAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::get("TeacherAgent", constructor_data)
  { client, }
}

///|
pub fn TeacherAgentClient::new_phantom() -> TeacherAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::new_phantom("TeacherAgent", constructor_data)
  { client, }
}

///|
pub fn TeacherAgentClient::get_phantom(phantom_id : @rpcTypes.Uuid) -> TeacherAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = 
    @rpc.AgentClient::get_phantom("TeacherAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn TeacherAgentClient::get_agent_id(self : TeacherAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn TeacherAgentClient::phantom_id(self : TeacherAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn TeacherAgentClient::drop(self : TeacherAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] TeacherAgentClient::scoped(
  f : (TeacherAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TeacherAgentClient::get()
  defer client.drop()
  f(client)
}

///|
pub fn[T] TeacherAgentClient::scoped_new_phantom(
  f : (TeacherAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TeacherAgentClient::new_phantom()
  defer client.drop()
  f(client)
}

///|
pub fn[T] TeacherAgentClient::scoped_get_phantom(
  phantom_id : @rpcTypes.Uuid,
  f : (TeacherAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TeacherAgentClient::get_phantom(phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn TeacherAgentClient::test_db(self : TeacherAgentClient) -> String raise @common.AgentError {
  let result = 
    self.client.invoke_and_await("test_db", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_test_db(self : TeacherAgentClient) -> Unit {
  self.client.invoke("test_db", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn TeacherAgentClient::schedule_test_db(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "test_db",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_test_db(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "test_db",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub(all) struct AdminAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn AdminAgentClient::get() -> AdminAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::get("AdminAgent", constructor_data)
  { client, }
}

///|
pub fn AdminAgentClient::new_phantom() -> AdminAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::new_phantom("AdminAgent", constructor_data)
  { client, }
}

///|
pub fn AdminAgentClient::get_phantom(phantom_id : @rpcTypes.Uuid) -> AdminAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = 
    @rpc.AgentClient::get_phantom("AdminAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn AdminAgentClient::get_agent_id(self : AdminAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn AdminAgentClient::phantom_id(self : AdminAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn AdminAgentClient::drop(self : AdminAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] AdminAgentClient::scoped(
  f : (AdminAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = AdminAgentClient::get()
  defer client.drop()
  f(client)
}

///|
pub fn[T] AdminAgentClient::scoped_new_phantom(
  f : (AdminAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = AdminAgentClient::new_phantom()
  defer client.drop()
  f(client)
}

///|
pub fn[T] AdminAgentClient::scoped_get_phantom(
  phantom_id : @rpcTypes.Uuid,
  f : (AdminAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = AdminAgentClient::get_phantom(phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn AdminAgentClient::ping(self : AdminAgentClient) -> String raise @common.AgentError {
  let result = 
    self.client.invoke_and_await("ping", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_ping(self : AdminAgentClient) -> Unit {
  self.client.invoke("ping", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn AdminAgentClient::schedule_ping(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(scheduled_at, "ping", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn AdminAgentClient::schedule_cancelable_ping(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "ping",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn AdminAgentClient::activate_user(
  self : AdminAgentClient,
  user_id : String,
  role : String,
  class_level : String?,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "activate_user",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(user_id),
        @schema.to_element_value_as(role),
        @schema.to_element_value_as(class_level),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_activate_user(
  self : AdminAgentClient,
  user_id : String,
  role : String,
  class_level : String?,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "activate_user",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(user_id),
        @schema.to_element_value_as(role),
        @schema.to_element_value_as(class_level),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_activate_user(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
  role : String,
  class_level : String?,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "activate_user",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(user_id),
        @schema.to_element_value_as(role),
        @schema.to_element_value_as(class_level),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_activate_user(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
  role : String,
  class_level : String?,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "activate_user",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(user_id),
        @schema.to_element_value_as(role),
        @schema.to_element_value_as(class_level),
      ]),
    )
}

///|
pub fn AdminAgentClient::is_user_active(
  self : AdminAgentClient,
  user_id : String,
) -> ActivationStatus raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "is_user_active",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_is_user_active(
  self : AdminAgentClient,
  user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "is_user_active",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
    )
}

///|
pub fn AdminAgentClient::schedule_is_user_active(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "is_user_active",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_is_user_active(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "is_user_active",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
    )
}

///|
pub fn AdminAgentClient::deactivate_user(
  self : AdminAgentClient,
  user_id : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "deactivate_user",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_deactivate_user(
  self : AdminAgentClient,
  user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "deactivate_user",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
    )
}

///|
pub fn AdminAgentClient::schedule_deactivate_user(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "deactivate_user",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_deactivate_user(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "deactivate_user",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
    )
}

///|
pub fn AdminAgentClient::get_all_activations(self : AdminAgentClient) -> Array[
  (String, ActivationStatus),
] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await("get_all_activations", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_all_activations(self : AdminAgentClient) -> Unit {
  self.client.invoke("get_all_activations", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn AdminAgentClient::schedule_get_all_activations(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_all_activations",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_all_activations(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_all_activations",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub(all) struct GatewayAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn GatewayAgentClient::new_phantom() -> GatewayAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::new_phantom("GatewayAgent", constructor_data)
  { client, }
}

///|
pub fn GatewayAgentClient::get_phantom(phantom_id : @rpcTypes.Uuid) -> GatewayAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = 
    @rpc.AgentClient::get_phantom("GatewayAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn GatewayAgentClient::get_agent_id(self : GatewayAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn GatewayAgentClient::phantom_id(self : GatewayAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn GatewayAgentClient::drop(self : GatewayAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] GatewayAgentClient::scoped_new_phantom(
  f : (GatewayAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = GatewayAgentClient::new_phantom()
  defer client.drop()
  f(client)
}

///|
pub fn[T] GatewayAgentClient::scoped_get_phantom(
  phantom_id : @rpcTypes.Uuid,
  f : (GatewayAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = GatewayAgentClient::get_phantom(phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn GatewayAgentClient::ping(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "ping",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(user_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_ping(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "ping",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_ping(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "ping",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_ping(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "ping",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::check_activation(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "check_activation",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(user_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_check_activation(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "check_activation",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_check_activation(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "check_activation",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_check_activation(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "check_activation",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::db_test(
  self : GatewayAgentClient,
  incoming_key : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "db_test",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_db_test(
  self : GatewayAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "db_test",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn GatewayAgentClient::schedule_db_test(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "db_test",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_db_test(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "db_test",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn GatewayAgentClient::activate_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
  role : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "activate_admin",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
        @schema.to_element_value_as(target_user_id),
        @schema.to_element_value_as(role),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_activate_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
  role : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "activate_admin",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
        @schema.to_element_value_as(target_user_id),
        @schema.to_element_value_as(role),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_activate_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
  role : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "activate_admin",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
        @schema.to_element_value_as(target_user_id),
        @schema.to_element_value_as(role),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_activate_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
  role : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "activate_admin",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
        @schema.to_element_value_as(target_user_id),
        @schema.to_element_value_as(role),
      ]),
    )
}

///|
pub fn GatewayAgentClient::deactivate_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "deactivate_admin",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_deactivate_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "deactivate_admin",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_deactivate_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "deactivate_admin",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_deactivate_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "deactivate_admin",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::list_activations(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "list_activations",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_list_activations(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "list_activations",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_list_activations(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "list_activations",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
      ]),
    )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_list_activations(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "list_activations",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(admin_user_id),
      ]),
    )
}

