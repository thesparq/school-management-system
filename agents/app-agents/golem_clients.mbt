// Generated by golem_sdk_tools — DO NOT EDIT!

///|
pub(all) struct GatewayAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn GatewayAgentClient::new_phantom() -> GatewayAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::new_phantom("GatewayAgent", constructor_data)
  { client, }
}

///|
pub fn GatewayAgentClient::get_phantom(
  phantom_id : @rpcTypes.Uuid,
) -> GatewayAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::get_phantom(
    "GatewayAgent", constructor_data, phantom_id,
  )
  { client, }
}

///|
pub fn GatewayAgentClient::get_agent_id(
  self : GatewayAgentClient,
) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn GatewayAgentClient::phantom_id(
  self : GatewayAgentClient,
) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn GatewayAgentClient::drop(self : GatewayAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] GatewayAgentClient::scoped_new_phantom(
  f : (GatewayAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = GatewayAgentClient::new_phantom()
  defer client.drop()
  f(client)
}

///|
pub fn[T] GatewayAgentClient::scoped_get_phantom(
  phantom_id : @rpcTypes.Uuid,
  f : (GatewayAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = GatewayAgentClient::get_phantom(phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn GatewayAgentClient::ping(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "ping",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_ping(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "ping",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_ping(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "ping",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_ping(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "ping",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::admin_ping(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "admin_ping",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_admin_ping(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "admin_ping",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_admin_ping(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "admin_ping",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_admin_ping(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "admin_ping",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::check_initialization(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "check_initialization",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_check_initialization(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "check_initialization",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_check_initialization(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "check_initialization",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_check_initialization(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "check_initialization",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::db_test(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "db_test",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_db_test(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "db_test",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_db_test(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "db_test",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_db_test(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "db_test",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::initialize_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
  role : String,
  class_level : String?,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "initialize_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_user_id),
      @schema.to_element_value_as(role),
      @schema.to_element_value_as(class_level),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_initialize_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
  role : String,
  class_level : String?,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "initialize_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_user_id),
      @schema.to_element_value_as(role),
      @schema.to_element_value_as(class_level),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_initialize_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
  role : String,
  class_level : String?,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "initialize_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_user_id),
      @schema.to_element_value_as(role),
      @schema.to_element_value_as(class_level),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_initialize_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_user_id : String,
  role : String,
  class_level : String?,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "initialize_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_user_id),
      @schema.to_element_value_as(role),
      @schema.to_element_value_as(class_level),
    ]),
  )
}

///|
pub fn GatewayAgentClient::student_subjects(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Result[Array[SubjectInfo], String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "student_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_student_subjects(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "student_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_student_subjects(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "student_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_student_subjects(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "student_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::student_terms(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Result[Array[TermInfo], String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "student_terms",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_student_terms(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "student_terms",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_student_terms(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "student_terms",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_student_terms(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "student_terms",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::student_lessons(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
  subject_id : String,
  term_id : String,
) -> Result[Array[LessonInfo], String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "student_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_student_lessons(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "student_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_student_lessons(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "student_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_student_lessons(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
  subject_id : String,
  term_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "student_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::student_lesson(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
  lesson_id : String,
) -> Result[LessonContent?, String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "student_lesson",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(lesson_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_student_lesson(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "student_lesson",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(lesson_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_student_lesson(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "student_lesson",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(lesson_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_student_lesson(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
  lesson_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "student_lesson",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(lesson_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::list_class_levels(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "list_class_levels",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_list_class_levels(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "list_class_levels",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_list_class_levels(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "list_class_levels",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_list_class_levels(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "list_class_levels",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::list_initializations(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "list_initializations",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_list_initializations(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "list_initializations",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_list_initializations(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "list_initializations",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_list_initializations(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "list_initializations",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::teacher_classes(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Result[Array[TeacherClassGroup], String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "teacher_classes",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_teacher_classes(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "teacher_classes",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_teacher_classes(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "teacher_classes",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_teacher_classes(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "teacher_classes",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::teacher_terms(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Result[Array[TermInfo], String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "teacher_terms",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_teacher_terms(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "teacher_terms",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_teacher_terms(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "teacher_terms",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_teacher_terms(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "teacher_terms",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::teacher_lessons(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> Result[Array[LessonInfo], String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "teacher_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(class_level_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_teacher_lessons(
  self : GatewayAgentClient,
  incoming_key : String,
  user_id : String,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "teacher_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(class_level_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_teacher_lessons(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "teacher_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(class_level_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_teacher_lessons(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  user_id : String,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "teacher_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(class_level_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::list_class_subjects(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> Result[Array[TeacherSubjectPair], String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "list_class_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_list_class_subjects(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "list_class_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_list_class_subjects(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "list_class_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_list_class_subjects(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "list_class_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::get_teacher_subjects_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_teacher_id : String,
) -> Result[Array[TeacherSubjectPair], String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_teacher_subjects_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_teacher_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_get_teacher_subjects_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_teacher_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "get_teacher_subjects_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_teacher_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_get_teacher_subjects_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_teacher_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "get_teacher_subjects_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_teacher_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_get_teacher_subjects_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_teacher_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_teacher_subjects_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_teacher_id),
    ]),
  )
}

///|
pub fn GatewayAgentClient::set_teacher_subjects_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_teacher_id : String,
  pairs_json : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "set_teacher_subjects_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_teacher_id),
      @schema.to_element_value_as(pairs_json),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn GatewayAgentClient::trigger_set_teacher_subjects_admin(
  self : GatewayAgentClient,
  incoming_key : String,
  admin_user_id : String,
  target_teacher_id : String,
  pairs_json : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "set_teacher_subjects_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_teacher_id),
      @schema.to_element_value_as(pairs_json),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_set_teacher_subjects_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_teacher_id : String,
  pairs_json : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "set_teacher_subjects_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_teacher_id),
      @schema.to_element_value_as(pairs_json),
    ]),
  )
}

///|
pub fn GatewayAgentClient::schedule_cancelable_set_teacher_subjects_admin(
  self : GatewayAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  admin_user_id : String,
  target_teacher_id : String,
  pairs_json : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "set_teacher_subjects_admin",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(incoming_key),
      @schema.to_element_value_as(admin_user_id),
      @schema.to_element_value_as(target_teacher_id),
      @schema.to_element_value_as(pairs_json),
    ]),
  )
}

///|
pub(all) struct TeacherAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn TeacherAgentClient::get(
  teacher_id : String,
) -> TeacherAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([
    @schema.to_element_value_as(teacher_id),
  ])
  let client = @rpc.AgentClient::get("TeacherAgent", constructor_data)
  { client, }
}

///|
pub fn TeacherAgentClient::new_phantom(
  teacher_id : String,
) -> TeacherAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([
    @schema.to_element_value_as(teacher_id),
  ])
  let client = @rpc.AgentClient::new_phantom("TeacherAgent", constructor_data)
  { client, }
}

///|
pub fn TeacherAgentClient::get_phantom(
  teacher_id : String,
  phantom_id : @rpcTypes.Uuid,
) -> TeacherAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([
    @schema.to_element_value_as(teacher_id),
  ])
  let client = @rpc.AgentClient::get_phantom(
    "TeacherAgent", constructor_data, phantom_id,
  )
  { client, }
}

///|
pub fn TeacherAgentClient::get_agent_id(
  self : TeacherAgentClient,
) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn TeacherAgentClient::phantom_id(
  self : TeacherAgentClient,
) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn TeacherAgentClient::drop(self : TeacherAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] TeacherAgentClient::scoped(
  teacher_id : String,
  f : (TeacherAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TeacherAgentClient::get(teacher_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] TeacherAgentClient::scoped_new_phantom(
  teacher_id : String,
  f : (TeacherAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TeacherAgentClient::new_phantom(teacher_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] TeacherAgentClient::scoped_get_phantom(
  teacher_id : String,
  phantom_id : @rpcTypes.Uuid,
  f : (TeacherAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TeacherAgentClient::get_phantom(teacher_id, phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn TeacherAgentClient::trigger_initialize(
  self : TeacherAgentClient,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "trigger_initialize",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_trigger_initialize(
  self : TeacherAgentClient,
) -> Unit {
  self.client.invoke("trigger_initialize", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn TeacherAgentClient::schedule_trigger_initialize(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "trigger_initialize",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_trigger_initialize(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "trigger_initialize",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn TeacherAgentClient::invalidate_cache(
  self : TeacherAgentClient,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "invalidate_cache",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_invalidate_cache(
  self : TeacherAgentClient,
) -> Unit {
  self.client.invoke("invalidate_cache", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn TeacherAgentClient::schedule_invalidate_cache(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "invalidate_cache",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_invalidate_cache(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "invalidate_cache",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn TeacherAgentClient::get_my_classes(
  self : TeacherAgentClient,
) -> Array[TeacherClassGroup] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_my_classes",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_get_my_classes(
  self : TeacherAgentClient,
) -> Unit {
  self.client.invoke("get_my_classes", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn TeacherAgentClient::schedule_get_my_classes(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "get_my_classes",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_get_my_classes(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_my_classes",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn TeacherAgentClient::get_terms(
  self : TeacherAgentClient,
) -> Array[TermInfo] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_terms",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_get_terms(self : TeacherAgentClient) -> Unit {
  self.client.invoke("get_terms", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn TeacherAgentClient::schedule_get_terms(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "get_terms",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_get_terms(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_terms",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn TeacherAgentClient::get_lessons(
  self : TeacherAgentClient,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> Array[LessonInfo] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(class_level_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_get_lessons(
  self : TeacherAgentClient,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "get_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(class_level_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn TeacherAgentClient::schedule_get_lessons(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "get_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(class_level_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_get_lessons(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(class_level_id),
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn TeacherAgentClient::test_db(
  self : TeacherAgentClient,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "test_db",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_test_db(self : TeacherAgentClient) -> Unit {
  self.client.invoke("test_db", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn TeacherAgentClient::schedule_test_db(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "test_db",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_test_db(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "test_db",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub(all) struct AdminAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn AdminAgentClient::get() -> AdminAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::get("AdminAgent", constructor_data)
  { client, }
}

///|
pub fn AdminAgentClient::new_phantom() -> AdminAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::new_phantom("AdminAgent", constructor_data)
  { client, }
}

///|
pub fn AdminAgentClient::get_phantom(
  phantom_id : @rpcTypes.Uuid,
) -> AdminAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::get_phantom(
    "AdminAgent", constructor_data, phantom_id,
  )
  { client, }
}

///|
pub fn AdminAgentClient::get_agent_id(
  self : AdminAgentClient,
) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn AdminAgentClient::phantom_id(self : AdminAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn AdminAgentClient::drop(self : AdminAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] AdminAgentClient::scoped(
  f : (AdminAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = AdminAgentClient::get()
  defer client.drop()
  f(client)
}

///|
pub fn[T] AdminAgentClient::scoped_new_phantom(
  f : (AdminAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = AdminAgentClient::new_phantom()
  defer client.drop()
  f(client)
}

///|
pub fn[T] AdminAgentClient::scoped_get_phantom(
  phantom_id : @rpcTypes.Uuid,
  f : (AdminAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = AdminAgentClient::get_phantom(phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn AdminAgentClient::ping(
  self : AdminAgentClient,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "ping",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_ping(self : AdminAgentClient) -> Unit {
  self.client.invoke("ping", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn AdminAgentClient::schedule_ping(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "ping",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::schedule_cancelable_ping(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "ping",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::initialize_user(
  self : AdminAgentClient,
  user_id : String,
  role : String,
  class_level : String?,
) -> Result[String, String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "initialize_user",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(role),
      @schema.to_element_value_as(class_level),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_initialize_user(
  self : AdminAgentClient,
  user_id : String,
  role : String,
  class_level : String?,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "initialize_user",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(role),
      @schema.to_element_value_as(class_level),
    ]),
  )
}

///|
pub fn AdminAgentClient::schedule_initialize_user(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
  role : String,
  class_level : String?,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "initialize_user",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(role),
      @schema.to_element_value_as(class_level),
    ]),
  )
}

///|
pub fn AdminAgentClient::schedule_cancelable_initialize_user(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
  role : String,
  class_level : String?,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "initialize_user",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(user_id),
      @schema.to_element_value_as(role),
      @schema.to_element_value_as(class_level),
    ]),
  )
}

///|
pub fn AdminAgentClient::is_user_initialized(
  self : AdminAgentClient,
  user_id : String,
) -> Bool raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "is_user_initialized",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_is_user_initialized(
  self : AdminAgentClient,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "is_user_initialized",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
  )
}

///|
pub fn AdminAgentClient::schedule_is_user_initialized(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "is_user_initialized",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
  )
}

///|
pub fn AdminAgentClient::schedule_cancelable_is_user_initialized(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "is_user_initialized",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(user_id)]),
  )
}

///|
pub fn AdminAgentClient::is_admin_initialized(
  self : AdminAgentClient,
) -> Bool raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "is_admin_initialized",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_is_admin_initialized(
  self : AdminAgentClient,
) -> Unit {
  self.client.invoke("is_admin_initialized", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn AdminAgentClient::schedule_is_admin_initialized(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "is_admin_initialized",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::schedule_cancelable_is_admin_initialized(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "is_admin_initialized",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::get_all_initialized(
  self : AdminAgentClient,
) -> Array[(String, String)] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_all_initialized",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_all_initialized(
  self : AdminAgentClient,
) -> Unit {
  self.client.invoke("get_all_initialized", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn AdminAgentClient::schedule_get_all_initialized(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "get_all_initialized",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_all_initialized(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_all_initialized",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::get_class_levels(
  self : AdminAgentClient,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_class_levels",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_class_levels(
  self : AdminAgentClient,
) -> Unit {
  self.client.invoke("get_class_levels", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn AdminAgentClient::schedule_get_class_levels(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "get_class_levels",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_class_levels(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_class_levels",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::get_available_class_subjects(
  self : AdminAgentClient,
) -> Array[TeacherSubjectPair] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_available_class_subjects",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_available_class_subjects(
  self : AdminAgentClient,
) -> Unit {
  self.client.invoke(
    "get_available_class_subjects",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::schedule_get_available_class_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "get_available_class_subjects",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_available_class_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_available_class_subjects",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn AdminAgentClient::set_teacher_subjects(
  self : AdminAgentClient,
  teacher_id : String,
  pairs : Array[TeacherSubjectPair],
) -> Result[String, String] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "set_teacher_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(teacher_id),
      @schema.to_element_value_as(pairs),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_set_teacher_subjects(
  self : AdminAgentClient,
  teacher_id : String,
  pairs : Array[TeacherSubjectPair],
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "set_teacher_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(teacher_id),
      @schema.to_element_value_as(pairs),
    ]),
  )
}

///|
pub fn AdminAgentClient::schedule_set_teacher_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  teacher_id : String,
  pairs : Array[TeacherSubjectPair],
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "set_teacher_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(teacher_id),
      @schema.to_element_value_as(pairs),
    ]),
  )
}

///|
pub fn AdminAgentClient::schedule_cancelable_set_teacher_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  teacher_id : String,
  pairs : Array[TeacherSubjectPair],
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "set_teacher_subjects",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(teacher_id),
      @schema.to_element_value_as(pairs),
    ]),
  )
}

///|
pub fn AdminAgentClient::get_teacher_subjects(
  self : AdminAgentClient,
  teacher_id : String,
) -> Array[TeacherSubjectPair] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_teacher_subjects",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(teacher_id)]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_teacher_subjects(
  self : AdminAgentClient,
  teacher_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "get_teacher_subjects",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(teacher_id)]),
  )
}

///|
pub fn AdminAgentClient::schedule_get_teacher_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  teacher_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "get_teacher_subjects",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(teacher_id)]),
  )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_teacher_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  teacher_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_teacher_subjects",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(teacher_id)]),
  )
}

///|
pub(all) struct StudentAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn StudentAgentClient::get(
  student_id : String,
) -> StudentAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([
    @schema.to_element_value_as(student_id),
  ])
  let client = @rpc.AgentClient::get("StudentAgent", constructor_data)
  { client, }
}

///|
pub fn StudentAgentClient::new_phantom(
  student_id : String,
) -> StudentAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([
    @schema.to_element_value_as(student_id),
  ])
  let client = @rpc.AgentClient::new_phantom("StudentAgent", constructor_data)
  { client, }
}

///|
pub fn StudentAgentClient::get_phantom(
  student_id : String,
  phantom_id : @rpcTypes.Uuid,
) -> StudentAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([
    @schema.to_element_value_as(student_id),
  ])
  let client = @rpc.AgentClient::get_phantom(
    "StudentAgent", constructor_data, phantom_id,
  )
  { client, }
}

///|
pub fn StudentAgentClient::get_agent_id(
  self : StudentAgentClient,
) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn StudentAgentClient::phantom_id(
  self : StudentAgentClient,
) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn StudentAgentClient::drop(self : StudentAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] StudentAgentClient::scoped(
  student_id : String,
  f : (StudentAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = StudentAgentClient::get(student_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] StudentAgentClient::scoped_new_phantom(
  student_id : String,
  f : (StudentAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = StudentAgentClient::new_phantom(student_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] StudentAgentClient::scoped_get_phantom(
  student_id : String,
  phantom_id : @rpcTypes.Uuid,
  f : (StudentAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = StudentAgentClient::get_phantom(student_id, phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn StudentAgentClient::test_db(
  self : StudentAgentClient,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "test_db",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_test_db(self : StudentAgentClient) -> Unit {
  self.client.invoke("test_db", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn StudentAgentClient::schedule_test_db(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "test_db",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn StudentAgentClient::schedule_cancelable_test_db(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "test_db",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn StudentAgentClient::initialize(
  self : StudentAgentClient,
  class_level : String,
) -> String raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "initialize",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_initialize(
  self : StudentAgentClient,
  class_level : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "initialize",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
  )
}

///|
pub fn StudentAgentClient::schedule_initialize(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "initialize",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
  )
}

///|
pub fn StudentAgentClient::schedule_cancelable_initialize(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "initialize",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
  )
}

///|
pub fn StudentAgentClient::get_subjects(
  self : StudentAgentClient,
) -> Array[SubjectInfo] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_subjects",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_get_subjects(
  self : StudentAgentClient,
) -> Unit {
  self.client.invoke("get_subjects", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn StudentAgentClient::schedule_get_subjects(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "get_subjects",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn StudentAgentClient::schedule_cancelable_get_subjects(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_subjects",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn StudentAgentClient::get_terms(
  self : StudentAgentClient,
) -> Array[TermInfo] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_terms",
    @rpcTypes.DataValue::Tuple([]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_get_terms(self : StudentAgentClient) -> Unit {
  self.client.invoke("get_terms", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn StudentAgentClient::schedule_get_terms(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self.client.schedule_invocation(
    scheduled_at,
    "get_terms",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn StudentAgentClient::schedule_cancelable_get_terms(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_terms",
    @rpcTypes.DataValue::Tuple([]),
  )
}

///|
pub fn StudentAgentClient::get_lessons(
  self : StudentAgentClient,
  subject_id : String,
  term_id : String,
) -> Array[LessonInfo] raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_get_lessons(
  self : StudentAgentClient,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "get_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn StudentAgentClient::schedule_get_lessons(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "get_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn StudentAgentClient::schedule_cancelable_get_lessons(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  subject_id : String,
  term_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_lessons",
    @rpcTypes.DataValue::Tuple([
      @schema.to_element_value_as(subject_id),
      @schema.to_element_value_as(term_id),
    ]),
  )
}

///|
pub fn StudentAgentClient::get_lesson(
  self : StudentAgentClient,
  lesson_id : String,
) -> LessonContent? raise @common.AgentError {
  let result = self.client.invoke_and_await(
    "get_lesson",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
  )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_get_lesson(
  self : StudentAgentClient,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self.client.invoke(
    "get_lesson",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
  )
}

///|
pub fn StudentAgentClient::schedule_get_lesson(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self.client.schedule_invocation(
    scheduled_at,
    "get_lesson",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
  )
}

///|
pub fn StudentAgentClient::schedule_cancelable_get_lesson(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self.client.schedule_cancelable_invocation(
    scheduled_at,
    "get_lesson",
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
  )
}
