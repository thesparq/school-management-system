// Generated by golem_sdk_tools — DO NOT EDIT!

///|
pub(all) struct AdminAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn AdminAgentClient::get(admin_id : String) -> AdminAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(admin_id)])
  let client = @rpc.AgentClient::get("AdminAgent", constructor_data)
  { client, }
}

///|
pub fn AdminAgentClient::new_phantom(admin_id : String) -> AdminAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(admin_id)])
  let client = @rpc.AgentClient::new_phantom("AdminAgent", constructor_data)
  { client, }
}

///|
pub fn AdminAgentClient::get_phantom(
  admin_id : String,
  phantom_id : @rpcTypes.Uuid,
) -> AdminAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(admin_id)])
  let client = 
    @rpc.AgentClient::get_phantom("AdminAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn AdminAgentClient::get_agent_id(self : AdminAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn AdminAgentClient::phantom_id(self : AdminAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn AdminAgentClient::drop(self : AdminAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] AdminAgentClient::scoped(
  admin_id : String,
  f : (AdminAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = AdminAgentClient::get(admin_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] AdminAgentClient::scoped_new_phantom(
  admin_id : String,
  f : (AdminAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = AdminAgentClient::new_phantom(admin_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] AdminAgentClient::scoped_get_phantom(
  admin_id : String,
  phantom_id : @rpcTypes.Uuid,
  f : (AdminAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = AdminAgentClient::get_phantom(admin_id, phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn AdminAgentClient::ping(self : AdminAgentClient, incoming_key : String) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_ping(
  self : AdminAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_ping(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_ping(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::get_profile(
  self : AdminAgentClient,
  incoming_key : String,
  target_user_id : String,
) -> Result[UserProfileInfo, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_profile",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_profile(
  self : AdminAgentClient,
  incoming_key : String,
  target_user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_profile",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_get_profile(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  target_user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_profile",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_profile(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  target_user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_profile",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::soft_delete_profile(
  self : AdminAgentClient,
  incoming_key : String,
  target_user_id : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "soft_delete_profile",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_soft_delete_profile(
  self : AdminAgentClient,
  incoming_key : String,
  target_user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "soft_delete_profile",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_soft_delete_profile(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  target_user_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "soft_delete_profile",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_soft_delete_profile(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  target_user_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "soft_delete_profile",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_user_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::get_class_levels(
  self : AdminAgentClient,
  incoming_key : String,
) -> Result[Array[ClassLevelInfo], String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_class_levels",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_class_levels(
  self : AdminAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_class_levels",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_get_class_levels(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_class_levels",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_class_levels(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_class_levels",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::get_available_class_subjects(
  self : AdminAgentClient,
  incoming_key : String,
) -> Result[Array[TeacherSubjectPair], String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_available_class_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_available_class_subjects(
  self : AdminAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_available_class_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_get_available_class_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_available_class_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_available_class_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_available_class_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::get_teacher_subjects(
  self : AdminAgentClient,
  incoming_key : String,
  target_teacher_id : String,
) -> Result[Array[TeacherSubjectPair], String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_teacher_subjects",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_teacher_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_teacher_subjects(
  self : AdminAgentClient,
  incoming_key : String,
  target_teacher_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_teacher_subjects",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_teacher_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_get_teacher_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  target_teacher_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_teacher_subjects",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_teacher_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_teacher_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  target_teacher_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_teacher_subjects",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_teacher_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::set_teacher_subjects(
  self : AdminAgentClient,
  incoming_key : String,
  target_teacher_id : String,
  pairs_json : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "set_teacher_subjects",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_teacher_id),
        @schema.to_element_value_as(pairs_json),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_set_teacher_subjects(
  self : AdminAgentClient,
  incoming_key : String,
  target_teacher_id : String,
  pairs_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "set_teacher_subjects",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_teacher_id),
        @schema.to_element_value_as(pairs_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_set_teacher_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  target_teacher_id : String,
  pairs_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "set_teacher_subjects",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_teacher_id),
        @schema.to_element_value_as(pairs_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_set_teacher_subjects(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  target_teacher_id : String,
  pairs_json : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "set_teacher_subjects",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(target_teacher_id),
        @schema.to_element_value_as(pairs_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::get_active_session_term(
  self : AdminAgentClient,
  incoming_key : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_active_session_term",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_active_session_term(
  self : AdminAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_active_session_term",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_get_active_session_term(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_active_session_term",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_active_session_term(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_active_session_term",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::get_terms(
  self : AdminAgentClient,
  incoming_key : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_terms(
  self : AdminAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_get_terms(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_terms(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::get_session_terms(
  self : AdminAgentClient,
  incoming_key : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_session_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_get_session_terms(
  self : AdminAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_session_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_get_session_terms(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_session_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_get_session_terms(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_session_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn AdminAgentClient::create_session_term(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "create_session_term",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_create_session_term(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "create_session_term",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_create_session_term(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "create_session_term",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_create_session_term(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "create_session_term",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::activate_session_term(
  self : AdminAgentClient,
  incoming_key : String,
  session_term_id : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "activate_session_term",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(session_term_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_activate_session_term(
  self : AdminAgentClient,
  incoming_key : String,
  session_term_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "activate_session_term",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(session_term_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_activate_session_term(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  session_term_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "activate_session_term",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(session_term_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_activate_session_term(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  session_term_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "activate_session_term",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(session_term_id),
      ]),
    )
}

///|
pub fn AdminAgentClient::invalidate_cache(
  self : AdminAgentClient,
  incoming_key : String,
  key : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_invalidate_cache(
  self : AdminAgentClient,
  incoming_key : String,
  key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_invalidate_cache(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_invalidate_cache(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
}

///|
pub fn AdminAgentClient::create_user_in_authentik(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Result[CreateUserResponse, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "create_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_create_user_in_authentik(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "create_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_create_user_in_authentik(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "create_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_create_user_in_authentik(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "create_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::edit_user_in_authentik(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "edit_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_edit_user_in_authentik(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "edit_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_edit_user_in_authentik(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "edit_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_edit_user_in_authentik(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "edit_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::delete_user_in_authentik(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "delete_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_delete_user_in_authentik(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "delete_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_delete_user_in_authentik(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "delete_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_delete_user_in_authentik(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "delete_user_in_authentik",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::set_user_active_status(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "set_user_active_status",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn AdminAgentClient::trigger_set_user_active_status(
  self : AdminAgentClient,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "set_user_active_status",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_set_user_active_status(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "set_user_active_status",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub fn AdminAgentClient::schedule_cancelable_set_user_active_status(
  self : AdminAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  body_json : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "set_user_active_status",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(body_json),
      ]),
    )
}

///|
pub(all) struct StudentAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn StudentAgentClient::get(student_id : String) -> StudentAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(student_id)])
  let client = @rpc.AgentClient::get("StudentAgent", constructor_data)
  { client, }
}

///|
pub fn StudentAgentClient::new_phantom(student_id : String) -> StudentAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(student_id)])
  let client = @rpc.AgentClient::new_phantom("StudentAgent", constructor_data)
  { client, }
}

///|
pub fn StudentAgentClient::get_phantom(
  student_id : String,
  phantom_id : @rpcTypes.Uuid,
) -> StudentAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(student_id)])
  let client = 
    @rpc.AgentClient::get_phantom("StudentAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn StudentAgentClient::get_agent_id(self : StudentAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn StudentAgentClient::phantom_id(self : StudentAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn StudentAgentClient::drop(self : StudentAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] StudentAgentClient::scoped(
  student_id : String,
  f : (StudentAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = StudentAgentClient::get(student_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] StudentAgentClient::scoped_new_phantom(
  student_id : String,
  f : (StudentAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = StudentAgentClient::new_phantom(student_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] StudentAgentClient::scoped_get_phantom(
  student_id : String,
  phantom_id : @rpcTypes.Uuid,
  f : (StudentAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = StudentAgentClient::get_phantom(student_id, phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn StudentAgentClient::ping(
  self : StudentAgentClient,
  incoming_key : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_ping(
  self : StudentAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn StudentAgentClient::schedule_ping(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn StudentAgentClient::schedule_cancelable_ping(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn StudentAgentClient::get_subjects(
  self : StudentAgentClient,
  incoming_key : String,
) -> Result[Array[SubjectInfo], String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_get_subjects(
  self : StudentAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn StudentAgentClient::schedule_get_subjects(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn StudentAgentClient::schedule_cancelable_get_subjects(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn StudentAgentClient::get_terms(
  self : StudentAgentClient,
  incoming_key : String,
) -> Result[Array[TermInfo], String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_get_terms(
  self : StudentAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn StudentAgentClient::schedule_get_terms(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn StudentAgentClient::schedule_cancelable_get_terms(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn StudentAgentClient::get_lessons(
  self : StudentAgentClient,
  incoming_key : String,
  subject_id : String,
  term_id : String,
) -> Result[Array[LessonInfo], String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(term_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_get_lessons(
  self : StudentAgentClient,
  incoming_key : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(term_id),
      ]),
    )
}

///|
pub fn StudentAgentClient::schedule_get_lessons(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(term_id),
      ]),
    )
}

///|
pub fn StudentAgentClient::schedule_cancelable_get_lessons(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  subject_id : String,
  term_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(term_id),
      ]),
    )
}

///|
pub fn StudentAgentClient::get_lesson(
  self : StudentAgentClient,
  incoming_key : String,
  lesson_id : String,
) -> Result[LessonContent?, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(lesson_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_get_lesson(
  self : StudentAgentClient,
  incoming_key : String,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(lesson_id),
      ]),
    )
}

///|
pub fn StudentAgentClient::schedule_get_lesson(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(lesson_id),
      ]),
    )
}

///|
pub fn StudentAgentClient::schedule_cancelable_get_lesson(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  lesson_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(lesson_id),
      ]),
    )
}

///|
pub fn StudentAgentClient::invalidate_cache(
  self : StudentAgentClient,
  incoming_key : String,
  key : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn StudentAgentClient::trigger_invalidate_cache(
  self : StudentAgentClient,
  incoming_key : String,
  key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
}

///|
pub fn StudentAgentClient::schedule_invalidate_cache(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
}

///|
pub fn StudentAgentClient::schedule_cancelable_invalidate_cache(
  self : StudentAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
}

///|
pub(all) struct TeacherAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn TeacherAgentClient::get(teacher_id : String) -> TeacherAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(teacher_id)])
  let client = @rpc.AgentClient::get("TeacherAgent", constructor_data)
  { client, }
}

///|
pub fn TeacherAgentClient::new_phantom(teacher_id : String) -> TeacherAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(teacher_id)])
  let client = @rpc.AgentClient::new_phantom("TeacherAgent", constructor_data)
  { client, }
}

///|
pub fn TeacherAgentClient::get_phantom(
  teacher_id : String,
  phantom_id : @rpcTypes.Uuid,
) -> TeacherAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(teacher_id)])
  let client = 
    @rpc.AgentClient::get_phantom("TeacherAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn TeacherAgentClient::get_agent_id(self : TeacherAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn TeacherAgentClient::phantom_id(self : TeacherAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn TeacherAgentClient::drop(self : TeacherAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] TeacherAgentClient::scoped(
  teacher_id : String,
  f : (TeacherAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TeacherAgentClient::get(teacher_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] TeacherAgentClient::scoped_new_phantom(
  teacher_id : String,
  f : (TeacherAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TeacherAgentClient::new_phantom(teacher_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] TeacherAgentClient::scoped_get_phantom(
  teacher_id : String,
  phantom_id : @rpcTypes.Uuid,
  f : (TeacherAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TeacherAgentClient::get_phantom(teacher_id, phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn TeacherAgentClient::ping(
  self : TeacherAgentClient,
  incoming_key : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_ping(
  self : TeacherAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn TeacherAgentClient::schedule_ping(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_ping(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "ping",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn TeacherAgentClient::get_my_classes(
  self : TeacherAgentClient,
  incoming_key : String,
) -> Result[Array[TeacherClassGroup], String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_my_classes",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_get_my_classes(
  self : TeacherAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_my_classes",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn TeacherAgentClient::schedule_get_my_classes(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_my_classes",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_get_my_classes(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_my_classes",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn TeacherAgentClient::get_terms(
  self : TeacherAgentClient,
  incoming_key : String,
) -> Result[Array[TermInfo], String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_get_terms(
  self : TeacherAgentClient,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn TeacherAgentClient::schedule_get_terms(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_get_terms(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_terms",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(incoming_key)]),
    )
}

///|
pub fn TeacherAgentClient::get_lessons(
  self : TeacherAgentClient,
  incoming_key : String,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> Result[Array[LessonInfo], String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(class_level_id),
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(term_id),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_get_lessons(
  self : TeacherAgentClient,
  incoming_key : String,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(class_level_id),
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(term_id),
      ]),
    )
}

///|
pub fn TeacherAgentClient::schedule_get_lessons(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(class_level_id),
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(term_id),
      ]),
    )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_get_lessons(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  class_level_id : String,
  subject_id : String,
  term_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(class_level_id),
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(term_id),
      ]),
    )
}

///|
pub fn TeacherAgentClient::invalidate_cache(
  self : TeacherAgentClient,
  incoming_key : String,
  key : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_invalidate_cache(
  self : TeacherAgentClient,
  incoming_key : String,
  key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
}

///|
pub fn TeacherAgentClient::schedule_invalidate_cache(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  key : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_invalidate_cache(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  key : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "invalidate_cache",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(key),
      ]),
    )
}

///|
pub fn TeacherAgentClient::toggle_term_active(
  self : TeacherAgentClient,
  incoming_key : String,
  term_id : String,
  active : Bool,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "toggle_term_active",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(term_id),
        @schema.to_element_value_as(active),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_toggle_term_active(
  self : TeacherAgentClient,
  incoming_key : String,
  term_id : String,
  active : Bool,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "toggle_term_active",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(term_id),
        @schema.to_element_value_as(active),
      ]),
    )
}

///|
pub fn TeacherAgentClient::schedule_toggle_term_active(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  term_id : String,
  active : Bool,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "toggle_term_active",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(term_id),
        @schema.to_element_value_as(active),
      ]),
    )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_toggle_term_active(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  term_id : String,
  active : Bool,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "toggle_term_active",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(term_id),
        @schema.to_element_value_as(active),
      ]),
    )
}

///|
pub fn TeacherAgentClient::toggle_lesson_active(
  self : TeacherAgentClient,
  incoming_key : String,
  lesson_id : String,
  active : Bool,
) -> Result[String, String] raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "toggle_lesson_active",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(active),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TeacherAgentClient::trigger_toggle_lesson_active(
  self : TeacherAgentClient,
  incoming_key : String,
  lesson_id : String,
  active : Bool,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "toggle_lesson_active",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(active),
      ]),
    )
}

///|
pub fn TeacherAgentClient::schedule_toggle_lesson_active(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  lesson_id : String,
  active : Bool,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "toggle_lesson_active",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(active),
      ]),
    )
}

///|
pub fn TeacherAgentClient::schedule_cancelable_toggle_lesson_active(
  self : TeacherAgentClient,
  scheduled_at : @wallClock.Datetime,
  incoming_key : String,
  lesson_id : String,
  active : Bool,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "toggle_lesson_active",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(incoming_key),
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(active),
      ]),
    )
}

