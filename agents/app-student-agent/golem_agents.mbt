// Generated by golem_sdk_tools — DO NOT EDIT!

fn init{
  @agents.register_agent({
    name: "TaskAgent",
    agent_type: {
      type_name: "TaskAgent",
      description: "Task agent in MoonBit",
      source_language: "moonbit",
      constructor_: {
        name: Some("new"),
        description: "Creates a new task agent with the given name",
        prompt_hint: None,
        input_schema: @rpcTypes.DataSchema::Tuple([
          (
            "name",
            @schema.schema_of_tag(
              (@schema.TypeTag::{  } : @schema.TypeTag[String]),
            ),
          ),
        ]),
      },
      methods: [
        {
          name: "create_task",
          description: "Creates a task and returns the complete task object",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Post,
              path_suffix: [@common.PathSegment::Literal("tasks")],
              header_vars: [],
              query_vars: [],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: Some("Create a new task with the given title"),
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "request",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[CreateTaskRequest]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[Task]),
              ),
            ),
          ]),
        },
        {
          name: "get_tasks",
          description: "Returns all tasks as a JSON array",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Get,
              path_suffix: [@common.PathSegment::Literal("tasks")],
              header_vars: [],
              query_vars: [],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: Some("List all existing tasks"),
          input_schema: @rpcTypes.DataSchema::Tuple([]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[Array[Task]]),
              ),
            ),
          ]),
        },
        {
          name: "complete_task",
          description: "Marks a task as completed by its ID",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Post,
              path_suffix: [
                @common.PathSegment::Literal("tasks"),
                @common.PathSegment::PathVariable({ variable_name: "id", }),
                @common.PathSegment::Literal("complete"),
              ],
              header_vars: [],
              query_vars: [],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "id",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[Int]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[Task?]),
              ),
            ),
          ]),
        },
      ],
      dependencies: [],
      mode: @common.AgentMode::DURABLE,
      http_mount: Some({
        path_prefix: [
          @common.PathSegment::Literal("task-agents"),
          @common.PathSegment::PathVariable({ variable_name: "name", }),
        ],
        auth_details: None,
        phantom_agent: false,
        cors_options: { allowed_patterns: [], },
        webhook_suffix: [],
      }),
      snapshotting: @common.Snapshotting::Disabled,
      config: [],
    },
    construct: fn(input, principal) {
      try {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let name :String = @schema.from_element_value_as(elements[0])
        let agent = TaskAgent::new(name)
        let constructed = @agents.ConstructedAgent::{
          raw: agent,
          snapshottable: None,
          snapshot_format: None,
        }
        Ok(constructed)
      } catch{
        e => Err(e)
      }
    },
  })
}

///|
impl @agents.RawAgent for TaskAgent with invoke(
  self,
  method_name,
  input,
  principal,
) {
  try {
    match method_name {
      "create_task" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let request :CreateTaskRequest = 
          @schema.from_element_value_as(elements[0])
        let result = self.create_task(request)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "get_tasks" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 0 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 0 elements, got " + elements.length().to_string(),
          )
        }
        let result = self.get_tasks()
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "complete_task" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let id :Int = @schema.from_element_value_as(elements[0])
        let result = self.complete_task(id)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      other => Err(@common.AgentError::InvalidMethod(other))
    }
  } catch{
    e => Err(e)
  }
}

