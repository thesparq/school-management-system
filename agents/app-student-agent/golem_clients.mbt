// Generated by golem_sdk_tools — DO NOT EDIT!

///|
pub(all) struct TaskAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn TaskAgentClient::get(name : String) -> TaskAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(name)])
  let client = @rpc.AgentClient::get("TaskAgent", constructor_data)
  { client, }
}

///|
pub fn TaskAgentClient::new_phantom(name : String) -> TaskAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(name)])
  let client = @rpc.AgentClient::new_phantom("TaskAgent", constructor_data)
  { client, }
}

///|
pub fn TaskAgentClient::get_phantom(name : String, phantom_id : @rpcTypes.Uuid) -> TaskAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(name)])
  let client = 
    @rpc.AgentClient::get_phantom("TaskAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn TaskAgentClient::get_agent_id(self : TaskAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn TaskAgentClient::phantom_id(self : TaskAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn TaskAgentClient::drop(self : TaskAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] TaskAgentClient::scoped(
  name : String,
  f : (TaskAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TaskAgentClient::get(name)
  defer client.drop()
  f(client)
}

///|
pub fn[T] TaskAgentClient::scoped_new_phantom(
  name : String,
  f : (TaskAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TaskAgentClient::new_phantom(name)
  defer client.drop()
  f(client)
}

///|
pub fn[T] TaskAgentClient::scoped_get_phantom(
  name : String,
  phantom_id : @rpcTypes.Uuid,
  f : (TaskAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = TaskAgentClient::get_phantom(name, phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn TaskAgentClient::create_task(
  self : TaskAgentClient,
  request : CreateTaskRequest,
) -> Task raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "create_task",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(request)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TaskAgentClient::trigger_create_task(
  self : TaskAgentClient,
  request : CreateTaskRequest,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "create_task",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(request)]),
    )
}

///|
pub fn TaskAgentClient::schedule_create_task(
  self : TaskAgentClient,
  scheduled_at : @wallClock.Datetime,
  request : CreateTaskRequest,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "create_task",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(request)]),
    )
}

///|
pub fn TaskAgentClient::schedule_cancelable_create_task(
  self : TaskAgentClient,
  scheduled_at : @wallClock.Datetime,
  request : CreateTaskRequest,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "create_task",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(request)]),
    )
}

///|
pub fn TaskAgentClient::get_tasks(self : TaskAgentClient) -> Array[Task] raise @common.AgentError {
  let result = 
    self.client.invoke_and_await("get_tasks", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TaskAgentClient::trigger_get_tasks(self : TaskAgentClient) -> Unit {
  self.client.invoke("get_tasks", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn TaskAgentClient::schedule_get_tasks(
  self : TaskAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_tasks",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn TaskAgentClient::schedule_cancelable_get_tasks(
  self : TaskAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_tasks",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn TaskAgentClient::complete_task(self : TaskAgentClient, id : Int) -> Task? raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "complete_task",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(id)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn TaskAgentClient::trigger_complete_task(self : TaskAgentClient, id : Int) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "complete_task",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(id)]),
    )
}

///|
pub fn TaskAgentClient::schedule_complete_task(
  self : TaskAgentClient,
  scheduled_at : @wallClock.Datetime,
  id : Int,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "complete_task",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(id)]),
    )
}

///|
pub fn TaskAgentClient::schedule_cancelable_complete_task(
  self : TaskAgentClient,
  scheduled_at : @wallClock.Datetime,
  id : Int,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "complete_task",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(id)]),
    )
}

