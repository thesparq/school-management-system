// Generated by golem_sdk_tools — DO NOT EDIT!

///|
pub(all) struct HumanAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn HumanAgentClient::get(username : String) -> HumanAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(username)])
  let client = @rpc.AgentClient::get("HumanAgent", constructor_data)
  { client, }
}

///|
pub fn HumanAgentClient::new_phantom(username : String) -> HumanAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(username)])
  let client = @rpc.AgentClient::new_phantom("HumanAgent", constructor_data)
  { client, }
}

///|
pub fn HumanAgentClient::get_phantom(
  username : String,
  phantom_id : @rpcTypes.Uuid,
) -> HumanAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(username)])
  let client = 
    @rpc.AgentClient::get_phantom("HumanAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn HumanAgentClient::get_agent_id(self : HumanAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn HumanAgentClient::phantom_id(self : HumanAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn HumanAgentClient::drop(self : HumanAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] HumanAgentClient::scoped(
  username : String,
  f : (HumanAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = HumanAgentClient::get(username)
  defer client.drop()
  f(client)
}

///|
pub fn[T] HumanAgentClient::scoped_new_phantom(
  username : String,
  f : (HumanAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = HumanAgentClient::new_phantom(username)
  defer client.drop()
  f(client)
}

///|
pub fn[T] HumanAgentClient::scoped_get_phantom(
  username : String,
  phantom_id : @rpcTypes.Uuid,
  f : (HumanAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = HumanAgentClient::get_phantom(username, phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn HumanAgentClient::decide(
  self : HumanAgentClient,
  workflow_id : String,
  decision : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "decide",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(workflow_id),
        @schema.to_element_value_as(decision),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn HumanAgentClient::trigger_decide(
  self : HumanAgentClient,
  workflow_id : String,
  decision : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "decide",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(workflow_id),
        @schema.to_element_value_as(decision),
      ]),
    )
}

///|
pub fn HumanAgentClient::schedule_decide(
  self : HumanAgentClient,
  scheduled_at : @wallClock.Datetime,
  workflow_id : String,
  decision : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "decide",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(workflow_id),
        @schema.to_element_value_as(decision),
      ]),
    )
}

///|
pub fn HumanAgentClient::schedule_cancelable_decide(
  self : HumanAgentClient,
  scheduled_at : @wallClock.Datetime,
  workflow_id : String,
  decision : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "decide",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(workflow_id),
        @schema.to_element_value_as(decision),
      ]),
    )
}

///|
pub(all) struct WorkflowAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn WorkflowAgentClient::get(workflow_id : String) -> WorkflowAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(workflow_id)])
  let client = @rpc.AgentClient::get("WorkflowAgent", constructor_data)
  { client, }
}

///|
pub fn WorkflowAgentClient::new_phantom(workflow_id : String) -> WorkflowAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(workflow_id)])
  let client = @rpc.AgentClient::new_phantom("WorkflowAgent", constructor_data)
  { client, }
}

///|
pub fn WorkflowAgentClient::get_phantom(
  workflow_id : String,
  phantom_id : @rpcTypes.Uuid,
) -> WorkflowAgentClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(workflow_id)])
  let client = 
    @rpc.AgentClient::get_phantom("WorkflowAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn WorkflowAgentClient::get_agent_id(self : WorkflowAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn WorkflowAgentClient::phantom_id(self : WorkflowAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn WorkflowAgentClient::drop(self : WorkflowAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] WorkflowAgentClient::scoped(
  workflow_id : String,
  f : (WorkflowAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = WorkflowAgentClient::get(workflow_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] WorkflowAgentClient::scoped_new_phantom(
  workflow_id : String,
  f : (WorkflowAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = WorkflowAgentClient::new_phantom(workflow_id)
  defer client.drop()
  f(client)
}

///|
pub fn[T] WorkflowAgentClient::scoped_get_phantom(
  workflow_id : String,
  phantom_id : @rpcTypes.Uuid,
  f : (WorkflowAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = WorkflowAgentClient::get_phantom(workflow_id, phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn WorkflowAgentClient::start(self : WorkflowAgentClient) -> String raise @common.AgentError {
  let result = 
    self.client.invoke_and_await("start", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn WorkflowAgentClient::trigger_start(self : WorkflowAgentClient) -> Unit {
  self.client.invoke("start", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn WorkflowAgentClient::schedule_start(
  self : WorkflowAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(scheduled_at, "start", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn WorkflowAgentClient::schedule_cancelable_start(
  self : WorkflowAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "start",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn WorkflowAgentClient::await_outcome(self : WorkflowAgentClient) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await("await_outcome", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn WorkflowAgentClient::trigger_await_outcome(self : WorkflowAgentClient) -> Unit {
  self.client.invoke("await_outcome", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn WorkflowAgentClient::schedule_await_outcome(
  self : WorkflowAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "await_outcome",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn WorkflowAgentClient::schedule_cancelable_await_outcome(
  self : WorkflowAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "await_outcome",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn WorkflowAgentClient::complete(
  self : WorkflowAgentClient,
  decision : String,
) -> Bool raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "complete",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(decision)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn WorkflowAgentClient::trigger_complete(
  self : WorkflowAgentClient,
  decision : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "complete",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(decision)]),
    )
}

///|
pub fn WorkflowAgentClient::schedule_complete(
  self : WorkflowAgentClient,
  scheduled_at : @wallClock.Datetime,
  decision : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "complete",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(decision)]),
    )
}

///|
pub fn WorkflowAgentClient::schedule_cancelable_complete(
  self : WorkflowAgentClient,
  scheduled_at : @wallClock.Datetime,
  decision : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "complete",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(decision)]),
    )
}

