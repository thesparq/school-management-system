// Generated by golem_sdk_tools — DO NOT EDIT!

fn init{
  @agents.register_agent({
    name: "MasterContentAgent",
    agent_type: {
      type_name: "MasterContentAgent",
      description: "Master content agent — orchestrates parallel content generation",
      source_language: "moonbit",
      constructor_: {
        name: Some("new"),
        description: "Creates a new MasterContentAgent",
        prompt_hint: None,
        input_schema: @rpcTypes.DataSchema::Tuple([]),
      },
      methods: [
        {
          name: "generate_all",
          description: "Generate content for all rows in a table",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Get,
              path_suffix: [@common.PathSegment::Literal("generate")],
              header_vars: [],
              query_vars: [
                { query_param_name: "table", variable_name: "table_name" },
              ],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "table_name",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (
                  @schema.TypeTag::{  }
                  : @schema.TypeTag[Result[String, AgentError]]
                ),
              ),
            ),
          ]),
        },
      ],
      dependencies: [],
      mode: @common.AgentMode::DURABLE,
      http_mount: Some({
        path_prefix: [@common.PathSegment::Literal("content")],
        auth_details: None,
        phantom_agent: false,
        cors_options: { allowed_patterns: [], },
        webhook_suffix: [],
      }),
      snapshotting: @common.Snapshotting::Disabled,
      config: [],
    },
    construct: fn(input, principal) {
      try {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 0 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 0 elements, got " + elements.length().to_string(),
          )
        }
        let agent = MasterContentAgent::new()
        let constructed = @agents.ConstructedAgent::{
          raw: agent,
          snapshottable: None,
          snapshot_format: None,
        }
        Ok(constructed)
      } catch{
        e => Err(e)
      }
    },
  })
  @agents.register_agent({
    name: "ChildContentAgent",
    agent_type: {
      type_name: "ChildContentAgent",
      description: "Child content agent — processes one topic row",
      source_language: "moonbit",
      constructor_: {
        name: Some("new"),
        description: "Creates a new ChildContentAgent",
        prompt_hint: None,
        input_schema: @rpcTypes.DataSchema::Tuple([
          (
            "id",
            @schema.schema_of_tag(
              (@schema.TypeTag::{  } : @schema.TypeTag[String]),
            ),
          ),
        ]),
      },
      methods: [
        {
          name: "generate",
          description: "Generate content for a single topic row",
          http_endpoint: [],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "table",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
            (
              "row_id",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([]),
        },
      ],
      dependencies: [],
      mode: @common.AgentMode::DURABLE,
      http_mount: None,
      snapshotting: @common.Snapshotting::Disabled,
      config: [],
    },
    construct: fn(input, principal) {
      try {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let id :String = @schema.from_element_value_as(elements[0])
        let agent = ChildContentAgent::new(id)
        let constructed = @agents.ConstructedAgent::{
          raw: agent,
          snapshottable: None,
          snapshot_format: None,
        }
        Ok(constructed)
      } catch{
        e => Err(e)
      }
    },
  })
}

///|
impl @agents.RawAgent for MasterContentAgent with invoke(
  self,
  method_name,
  input,
  principal,
) {
  try {
    match method_name {
      "generate_all" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let table_name :String = @schema.from_element_value_as(elements[0])
        let result = self.generate_all(table_name)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      other => Err(@common.AgentError::InvalidMethod(other))
    }
  } catch{
    e => Err(e)
  }
}

///|
impl @agents.RawAgent for ChildContentAgent with invoke(
  self,
  method_name,
  input,
  principal,
) {
  try {
    match method_name {
      "generate" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 2 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 2 elements, got " + elements.length().to_string(),
          )
        }
        let table :String = @schema.from_element_value_as(elements[0])
        let row_id :String = @schema.from_element_value_as(elements[1])
        self.generate(table, row_id)
        Ok(@rpcTypes.DataValue::Tuple([]))
      }
      other => Err(@common.AgentError::InvalidMethod(other))
    }
  } catch{
    e => Err(e)
  }
}

